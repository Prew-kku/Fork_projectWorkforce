DROP TABLE IF EXISTS appointment;
DROP TABLE IF EXISTS doctor_profile;
DROP TABLE IF EXISTS doctor;
DROP TABLE IF EXISTS patient;
DROP TABLE IF EXISTS roles;

-- สร้างตาราง roles ก่อน
CREATE TABLE roles (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL
);

-- ตอนนี้ table roles ถูกสร้างแล้ว จึงสามารถ insert ข้อมูลได้
INSERT INTO roles (id, name) VALUES (1, 'ROLE_PATIENT'), (2, 'ROLE_DOCTOR');

-- ตาราง doctor
CREATE TABLE doctor (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    password VARCHAR(100) NOT NULL,
    name VARCHAR(100),
    specialization VARCHAR(100)
);

-- ตาราง doctor_profile
CREATE TABLE doctor_profile (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    license_number VARCHAR(50),
    biography VARCHAR(255),
    doctor_id BIGINT UNIQUE,
    CONSTRAINT fk_profile_doctor FOREIGN KEY (doctor_id) REFERENCES doctor(id)
);

-- ตาราง patient
CREATE TABLE patient (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    password VARCHAR(100) NOT NULL,
    name VARCHAR(100),
    phone VARCHAR(20)
);

-- ตาราง appointment
CREATE TABLE appointment (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    appointment_date TIMESTAMP,
    status VARCHAR(20),
    doctor_id BIGINT,
    patient_id BIGINT,
    CONSTRAINT fk_appt_doctor FOREIGN KEY (doctor_id) REFERENCES doctor(id),
    CONSTRAINT fk_appt_patient FOREIGN KEY (patient_id) REFERENCES patient(id)
);
